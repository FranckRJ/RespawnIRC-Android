<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/SelectableItemBackgroundBorderless"
    android:layout_width="match_parent"
    android:layout_height="@dimen/sizeOfColorPickerCell"
    android:padding="4dp">

    <ImageView
        android:id="@+id/color_colorpicker_row"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:src="@drawable/colorpicker_swatch"/>

    <ImageView
        android:id="@+id/checkmark_colorpicker_row"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:src="@drawable/colorpicker_checkmark"
        android:visibility="gone"/>

</FrameLayout>
